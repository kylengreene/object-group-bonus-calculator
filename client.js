const employees = [
  {
    name: 'Atticus',
    employeeNumber: '2405',
    annualSalary: '47000',
    reviewRating: 3
  },
  {
    name: 'Jem',
    employeeNumber: '62347',
    annualSalary: '63500',
    reviewRating: 4
  },
  {
    name: 'Scout',
    employeeNumber: '6243',
    annualSalary: '74750',
    reviewRating: 5
  },
  {
    name: 'Robert',
    employeeNumber: '26835',
    annualSalary: '66000',
    reviewRating: 1
  },
  {
    name: 'Mayella',
    employeeNumber: '89068',
    annualSalary: '35000',
    reviewRating: 1
  }
];

// YOU SHOULD NOT NEED TO CHANGE ANYTHING ABOVE THIS POINT

// Take small steps! Don't write a for loop and two functions that do all of the calculations right away.
// This problem is massive! Break the problem down. Use the debugger.
// What is the fewest lines of code I can write and test to get just a little closer?

// This is not a race. Everyone on your team should understand what is happening.
// Ask questions when you don't.

console.log( employees );

function bonusCalculation(employeeToCalculate){
 let calculatedEmployee={};
employeeToCalculate.name=calculatedEmployee.name;
  
  employeeToCalculate
  employeeToCalculate

}

for(i=0;i<employees.length;i++){

}

let testPerson = {
  name: 'Mayella',
  employeeNumber: '89068',
  annualSalary: '35000',
  reviewRating: 1
}

function test(person){
  let newobj = {};
//No years and no 65000
  if(employeeToCalculate.reviewRating<=2){
  newobj.name=person.name,
  newobj.bonusPercentage=0,
  newobj.totalCompensation=obj.salary,
  newobj.totalBonus=0}
  return newobj
}

  if (employeeToCalculate.reviewRating == 3) {
    newobj.name = obj.name
    newobj.salary = (obj.salary * 1.04)
    return newobj

  if (employeeToCalculate.reviewRating == 4) {
      newobj.name = obj.name
      newobj.salary = (obj.salary * 1.06)
      return newobj


    if (employeeToCalculate.reviewRating == 5) {
      newobj.name = obj.name
      newobj.salary = (obj.salary * 1.10)
      return newobj


//yes years, no 65000 
      if (employeeToCalculate.employeeNumber < 10000) {

      if (employeeToCalculate.reviewRating <= 2) {
        newobj.name = obj.name
        newobj.salary = (obj.salary)
        return newobj
      }

      if (employeeToCalculate.reviewRating == 3) {
        newobj.name = obj.name
        newobj.salary = (obj.salary * 1.09)
        return newobj

        if (employeeToCalculate.reviewRating == 4) {
          newobj.name = obj.name
          newobj.salary = (obj.salary * 1.11)
          return newobj


          if (employeeToCalculate.reviewRating == 5) {
            newobj.name = obj.name
            newobj.salary = (obj.salary * 1.13)
            return newobj

  // No years yes 6500
            if (employeeToCalculate.reviewRating <= 2) {
              newobj.name = obj.name
              newobj.salary = (obj.salary)
              return newobj
            }

            if (employeeToCalculate.reviewRating == 3) {
              newobj.name = obj.name
              newobj.salary = (obj.salary * 1.03)
              return newobj

              if (employeeToCalculate.reviewRating == 4) {
                newobj.name = obj.name
                newobj.salary = (obj.salary * 1.05)
                return newobj


                if (employeeToCalculate.reviewRating == 5) {
                  newobj.name = obj.name
                  newobj.salary = (obj.salary * 1.09)
                  return newobj
//YES YEARS YES 65
                  if (employeeToCalculate.reviewRating <= 2) {
                    newobj.name = obj.name
                    newobj.salary = (obj.salary)
                    return newobj
                  }

                  if (employeeToCalculate.reviewRating == 3) {
                    newobj.name = obj.name
                    newobj.salary = (obj.salary * 1.08)
                    return newobj

                    if (employeeToCalculate.reviewRating == 4) {
                      newobj.name = obj.name
                      newobj.salary = (obj.salary * 1.10)
                      return newobj


                      if (employeeToCalculate.reviewRating == 5) {
                        newobj.name = obj.name
                        newobj.salary = (obj.salary * 1.13)
                        return newobj
                      }
}
